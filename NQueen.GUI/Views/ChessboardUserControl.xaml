<UserControl
    x:Class="NQueen.GUI.Views.ChessboardUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NQueen.GUI.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:NQueen.Kernel.Utilities;assembly=NQueen.Kernel"
    xmlns:viewmodels="clr-namespace:NQueen.GUI.ViewModels"
    x:Name="ChessboardControl"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:NullImageConverter x:Key="NullImageConverter" />
    </UserControl.Resources>

    <Grid
        x:Name="ChessboardGrid"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
        <ItemsControl x:Name="ChessboardItemsControl" ItemsSource="{Binding Chessboard.Squares}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="0"
                        Background="{Binding Color}"
                        BorderBrush="Black"
                        BorderThickness="0">
                        <Image Source="{Binding ImagePath, Converter={StaticResource NullImageConverter}}" Stretch="Uniform" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
